┌───────────────────────────┐
|       AuthController      |
|---------------------------|
| + signup(body)           |
| + login(body)            |
| + getProfile(req)        |
| + checkEmail(email)      |
└─────────────▲─────────────┘
              |
              |
       ┌──────┴────────┐
       |   AuthService |
       |---------------|
       | + signup()    |
       | + login()     |
       | + checkEmailExists() |
       └──────┬────────┘
              |
              |
       ┌──────┴───────────────┐
       |      PrismaService   |
       |----------------------|
       | + $connect()         |
       | + user.create()      |
       | + user.findUnique()  |
       └──────────────────────┘

┌────────────────────────────┐
|      JwtStrategy           |
|----------------------------|
| + validate(payload)        |
└────────────────────────────┘

클래스 다이어그램